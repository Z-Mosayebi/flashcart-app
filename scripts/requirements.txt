# Dependencies for scripts/try_generate.py, which runs the card-generation
# pipeline offline against a downloaded notes document.
#
# The script imports the AI service's own modules, so install this alongside
# ai-service/requirements.txt rather than instead of it.
python-dotenv>=1.0.1
