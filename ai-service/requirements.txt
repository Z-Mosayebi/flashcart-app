# Minimum versions rather than exact pins: the previously pinned
# pydantic==2.9.2 has no prebuilt wheel for Python 3.13+, so pip fell back to
# compiling pydantic-core from Rust source and failed on a stock Windows box.
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
python-dotenv>=1.0.1
httpx>=0.27.2

# Only needed when LLM_PROVIDER=anthropic. Gemini uses httpx directly, so this
# can be omitted on a free-tier deployment.
anthropic>=0.34.2
